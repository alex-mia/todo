import 'dart:ui';
import 'package:flutter/material.dart';


class StateProjects {
  bool hasText;
  String? text;
  Color? color;
  IconData? icon;

  StateProjects(this.hasText, this.text, this.color, this.icon);
}